name: Deploy Staging

on:
  push:
    tags:
      - "staging-*"

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    uses: ./.github/workflows/build-and-deploy.yml
    with:
      environment: staging
    permissions:
      id-token: write
      contents: read
